# Local backups and scratch work
.backups/
nimbalyst-local/
server.log
auth.thml

# Documentation and debug screenshots — not needed at runtime.
# Listed explicitly so public/images/* (served by the app) is never excluded.
docs/
images/
error1.png
error2.png
start.png

# Tests and test tooling
tests/
vitest.config.js

# Stale artifacts
*.bak
*.old

# Alternate package manager files (package-lock.json is authoritative)
pnpm-lock.yaml
pnpm-workspace.yaml

# Local Vercel state (contains pulled env values)
.vercel/
